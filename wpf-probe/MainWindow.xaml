<Window x:Class="dotnet_sso_wpf_probe.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,8" >
            <TextBlock Text="Tenant ID:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="TenantIdTextBox" Width="220" Height="24" Margin="0,0,12,0" ToolTip="Optional - leave empty to use default/common tenant" />
            <TextBlock Text="Client ID:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="ClientIdTextBox" Width="320" Height="24" Margin="0,0,12,0" Text="YOUR_CLIENT_ID" />
            <Button x:Name="LoginButton" Width="80" Height="28" Margin="0,0,8,0" Click="LoginButton_Click">Login</Button>
            <Button x:Name="LogoutButton" Width="80" Height="28" Margin="0,0,8,0" Click="LogoutButton_Click">Logout</Button>
            <TextBlock x:Name="StatusTextBlock" VerticalAlignment="Center" Text="Not logged in" />
        </StackPanel>

        <TextBox x:Name="LogTextBox"
                 Grid.Row="1"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12" />
    </Grid>
</Window>